#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {
  // Printeaza outpul-ul $ fara buffering
  setbuf(stdout, NULL);

  // TODO: Uncomment the code below to pass the first stage
  printf("$ ");

  return 0;
}
